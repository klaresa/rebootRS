import React from 'react'; // importa o React
import { render } from 'react-dom'; // importa o render de dentro do react-dom

import App from './App';

render(
    <App/>,
    document.getElementById('app')
);
